{
  "name": "jarvis",
  "version": "0.1.0",
  "bin": {
    "jarvis": "bin/jarvis.js"
  },
  "scripts": {
    "build": "cd infra && tsc",
    "watch": "cd infra && tsc -w",
    "test": "jest",
    "cdk": "cd infra && cdk",
    "deploy": "cd infra && tsc && cdk deploy --require-approval never",
    "destroy": "cd infra && cdk destroy"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "aws-cdk": "2.1027.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.211.0",
    "constructs": "^10.0.0"
  }
}
