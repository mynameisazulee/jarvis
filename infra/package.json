{
  "name": "jarvis-infra",
  "private": true,
  "scripts": {
    "build": "tsc",
    "deploy": "tsc && cdk deploy --require-approval never",
    "destroy": "cdk destroy"
  },
  "dependencies": {
    "aws-cdk-lib": "2.211.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "22.7.9",
    "typescript": "~5.6.3"
  }
}