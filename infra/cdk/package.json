{
  "name": "jarvis-cdk",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "synth": "npm run build && cdk synth",
    "deploy": "npm run build && cdk deploy --require-approval never",
    "destroy": "cdk destroy"
  },
  "dependencies": {
    "aws-cdk-lib": "2.151.0",
    "constructs": "10.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.5.4"
  }
}
